interface:
  display_name: "Strengthen Tests"
  short_description: "Improve tests to catch meaningful regressions"
  default_prompt: "Use $strengthen-tests to improve these tests so they exercise real production behavior."
