interface:
  display_name: "Change Summary"
  short_description: "Summarize changes for review"
  default_prompt: "Use $change-summary to explain the current changes and verification, with detail proportional to the diff."
